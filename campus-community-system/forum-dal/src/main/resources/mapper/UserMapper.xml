<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.forum.dal.mapper.UserMapper">

    <sql id="selectFields">
        id, username, password, salt, email, type, status, activation_code, avatar_url, introduction, security_question, security_answer, create_time
    </sql>

    <sql id="insertFields">
        username, password, salt, email, type, status, activation_code, avatar_url, introduction, create_time
    </sql>

    <select id="selectById" resultType="com.campus.forum.dal.domain.User">
        select <include refid="selectFields"></include>
        from user
        where id = #{id}
    </select>

    <select id="selectByName" resultType="com.campus.forum.dal.domain.User">
        select <include refid="selectFields"></include>
        from user
        where username = #{username}
    </select>

    <select id="selectByEmail" resultType="com.campus.forum.dal.domain.User">
        select <include refid="selectFields"></include>
        from user
        where email = #{email}
    </select>

    <insert id="insertUser" parameterType="com.campus.forum.dal.domain.User" keyProperty="id">
        insert into user(<include refid="insertFields"></include>)
        values(#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{avatarUrl}, #{introduction}, #{createTime})
    </insert>

    <update id="updateStatus">
        update user set status = #{status} where id = #{id}
    </update>

    <update id="updateAvatar">
        update user set avatar_url = #{avatarUrl} where id = #{id}
    </update>

    <update id="updatePassword">
        update user set password = #{password} where id = #{id}
    </update>
    <update id="updateIntroduction">
        update user set introduction = #{introduction} where id = #{id}
    </update>

    <update id="updateEmail">
        update user set email = #{email} where id = #{id}
    </update>

    <update id="updateUsername">
        update user set username = #{username} where id = #{id}
    </update>

    <update id="updateSecurityQuestion">
        update user set 
            security_question = #{question},
            security_answer = #{answer}
        where id = #{id}
    </update>

    <select id="countUsers" resultType="int">
        select count(*) from user
    </select>

    <select id="selectAllUsers" resultType="com.campus.forum.dal.domain.User">
        select <include refid="selectFields"></include>
        from user
        order by create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="selectActiveUsers" resultType="com.campus.forum.dal.domain.User">
        select u.id, u.username, u.password, u.salt, u.email, u.type, u.status, 
               u.activation_code, u.avatar_url, u.introduction, u.security_question, 
               u.security_answer, u.create_time,
               COALESCE(p.post_count, 0) as postCount,
               COALESCE(c.comment_count, 0) as commentCount
        from user u
        left join (
            select user_id, count(*) as post_count 
            from post 
            where status = 0 
            group by user_id
        ) p on u.id = p.user_id
        left join (
            select user_id, count(*) as comment_count 
            from comment 
            where status = 0 
            group by user_id
        ) c on u.id = c.user_id
        where u.status = 1
        order by (COALESCE(p.post_count, 0) + COALESCE(c.comment_count, 0)) desc
        limit #{offset}, #{limit}
    </select>

    <select id="selectActiveUsersByPeriod" resultType="com.campus.forum.dal.domain.User">
        select u.id, u.username, u.password, u.salt, u.email, u.type, u.status, 
               u.activation_code, u.avatar_url, u.introduction, u.security_question, 
               u.security_answer, u.create_time,
               COALESCE(p.post_count, 0) as postCount,
               COALESCE(c.comment_count, 0) as commentCount
        from user u
        left join (
            select user_id, count(*) as post_count 
            from post 
            where status = 0 
            <if test="period == 'daily'">
                and create_time >= DATE_SUB(NOW(), INTERVAL 1 DAY)
            </if>
            <if test="period == 'weekly'">
                and create_time >= DATE_SUB(NOW(), INTERVAL 1 WEEK)
            </if>
            <if test="period == 'monthly'">
                and create_time >= DATE_SUB(NOW(), INTERVAL 1 MONTH)
            </if>
            group by user_id
        ) p on u.id = p.user_id
        left join (
            select user_id, count(*) as comment_count 
            from comment 
            where status = 0 
            <if test="period == 'daily'">
                and create_time >= DATE_SUB(NOW(), INTERVAL 1 DAY)
            </if>
            <if test="period == 'weekly'">
                and create_time >= DATE_SUB(NOW(), INTERVAL 1 WEEK)
            </if>
            <if test="period == 'monthly'">
                and create_time >= DATE_SUB(NOW(), INTERVAL 1 MONTH)
            </if>
            group by user_id
        ) c on u.id = c.user_id
        where u.status = 1
        order by (COALESCE(p.post_count, 0) + COALESCE(c.comment_count, 0)) desc
        limit #{offset}, #{limit}
    </select>

</mapper>

