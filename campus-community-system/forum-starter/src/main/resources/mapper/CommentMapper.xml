<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.forum.dal.mapper.CommentMapper">
    
    <resultMap id="CommentWithUser" type="com.campus.forum.dal.domain.Comment">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="entityType" column="entity_type"/>
        <result property="entityId" column="entity_id"/>
        <result property="targetId" column="target_id"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="likeCount" column="like_count"/>
        <result property="createTime" column="create_time"/>
        <association property="user" javaType="com.campus.forum.dal.domain.User">
            <id property="id" column="u_id"/>
            <result property="username" column="username"/>
            <result property="avatarUrl" column="avatar_url"/>
        </association>
    </resultMap>

    <sql id="selectFields">
        id, user_id, entity_type, entity_id, target_id, content, status, like_count, create_time
    </sql>

    <sql id="insertFields">
        user_id, entity_type, entity_id, target_id, content, status, like_count, create_time
    </sql>
    <delete id="delById">
        DELETE FROM `comment` WHERE id = #{id}
    </delete>

    <select id="selectCommentsByEntity" resultMap="CommentWithUser">
        select c.id, c.user_id, c.entity_type, c.entity_id, c.target_id, c.content, c.status, c.like_count, c.create_time,
               u.id as u_id, u.username, u.avatar_url
        from comment c
        left join user u on c.user_id = u.id
        where c.status = 0
        and c.entity_type = #{entityType}
        and c.entity_id = #{entityId}
        order by c.create_time asc
    </select>

    <select id="countCommentByEntity" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM `comment`
        WHERE entity_type = #{entityType} AND entity_id = #{entityId} AND status = 0

    </select>
    <select id="selectByIdComment" resultType="com.campus.forum.dal.domain.Comment">
        SELECT <include refid="selectFields"></include>
        FROM comment
        WHERE id = #{id}
    </select>

    <insert id="insertComment" parameterType="com.campus.forum.dal.domain.Comment" keyProperty="id">
        insert into comment(<include refid="insertFields"></include>)
        values(#{userId}, #{entityType}, #{entityId}, #{targetId}, #{content}, #{status}, #{likeCount}, #{createTime})
    </insert>

    <update id="updateStatus">
        update comment set status = #{status} where id = #{id}
    </update>

    <!-- 我的评论 -->
    <select id="selectCommentsByUser" resultMap="CommentWithUser">
        select c.id,c.user_id,c.entity_type,c.entity_id,c.target_id,c.content,c.status,c.like_count,c.create_time,
               u.id as u_id,u.username,u.avatar_url
        from comment c
                 left join user u on c.user_id=u.id
        where c.status=0 and c.user_id=#{userId}
        order by c.create_time desc
    </select>

    <!-- 评论了我的帖子：对帖子(entity_type=1)的评论，且该帖是我发的 -->
    <select id="selectCommentsOnMyPosts" resultMap="CommentWithUser">
        select c.id,c.user_id,c.entity_type,c.entity_id,c.target_id,c.content,c.status,c.like_count,c.create_time,
               u.id as u_id,u.username,u.avatar_url
        from comment c
                 join post p on p.id=c.entity_id and c.entity_type=1
                 left join user u on c.user_id=u.id
        where c.status=0 and p.user_id=#{myUserId}
        order by c.create_time desc
    </select>

    <!-- 回复了我：对评论(entity_type=2)的评论，且target_id=我 -->
    <select id="selectRepliesToMe" resultMap="CommentWithUser">
        select c.id,c.user_id,c.entity_type,c.entity_id,c.target_id,c.content,c.status,c.like_count,c.create_time,
               u.id as u_id,u.username,u.avatar_url
        from comment c
                 left join user u on c.user_id=u.id
        where c.status=0 and c.entity_type=2 and c.target_id=#{myUserId}
        order by c.create_time desc
    </select>

    <select id="selectByUserId" resultType="com.campus.forum.dal.domain.Comment">
        SELECT id, user_id, post_id, content, create_time
        FROM comment
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="countComments" resultType="int">
        select count(*) from comment where status != 1
    </select>

    <!-- 管理员专用查询 -->
    <select id="selectAllComments" resultMap="CommentWithUser">
        select c.id, c.user_id, c.entity_type, c.entity_id, c.target_id, c.content, c.status, c.like_count, c.create_time,
               u.id as u_id, u.username, u.avatar_url
        from comment c
        left join user u on c.user_id = u.id
        order by c.create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="searchComments" resultMap="CommentWithUser">
        select c.id, c.user_id, c.entity_type, c.entity_id, c.target_id, c.content, c.status, c.like_count, c.create_time,
               u.id as u_id, u.username, u.avatar_url
        from comment c
        left join user u on c.user_id = u.id
        where c.content like concat('%', #{keyword}, '%') or u.username like concat('%', #{keyword}, '%')
        order by c.create_time desc
        limit #{offset}, #{limit}
    </select>

    <delete id="batchDeleteComments">
        delete from comment where id in
        <foreach collection="commentIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>


